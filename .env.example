# Local Ollama on Thor (preferred). DeepSeek is optional cloud fallback.
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL=qwen2.5:7b

# DeepSeek (cloud planner) — do not commit the real key
DEEPSEEK_API_KEY=
LLM_MODEL=deepseek-v4-flash
LLM_BASE_URL=https://api.deepseek.com
